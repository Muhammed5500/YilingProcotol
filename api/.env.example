# Hub Contract (Monad)
RPC_URL=https://testnet-rpc.monad.xyz
SKC_ENGINE_ADDRESS=0x...
AGENT_REGISTRY_ADDRESS=0x...
REPUTATION_MANAGER_ADDRESS=0x...

# Protocol API wallet (calls Hub contract)
PRIVATE_KEY=0x...

# x402 — Multi-chain facilitators
X402_FACILITATOR_URL=https://api.cdp.coinbase.com/platform/v2/x402
X402_FACILITATOR_FALLBACK_URL=https://www.x402.org/facilitator
MONAD_FACILITATOR_URL=https://x402-facilitator.molandak.org
TREASURY_ADDRESS=0x...

# Server
NETWORK_ENV=testnet
PORT=3001

# ────────────────────────────────────────────────────────────
# Test wallets — used by api/test/* scripts only
# Testnet keys, but still: do not reuse on mainnet, do not commit
# ────────────────────────────────────────────────────────────

# Builder wallet (creates queries in test scripts; needs Base Sepolia + Monad USDC)
TEST_BUILDER_KEY=0x...

# Agent wallets used by test-full-base.ts and test-x402-flow.ts
TEST_ANALYST_KEY=0x...
TEST_CONTRARIAN_KEY=0x...
TEST_OPTIMIST_KEY=0x...
