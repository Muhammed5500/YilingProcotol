# Deployer wallet (signs the transactions sent by Deploy.s.sol)
PRIVATE_KEY=0x...

# Role addresses — each should be a separate wallet from the deployer
PROTOCOL_API_ADDRESS=0x...
TREASURY_ADDRESS=0x...
